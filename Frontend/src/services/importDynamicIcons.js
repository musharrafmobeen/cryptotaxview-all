export const getBigButtonIcon = (name) => {
  const icon = require(`../resources/design-icons/professionalPlan-icons/${name}`);
  return icon;
};

export const getPlanIcon = (name) => {
  const icon = require(`../resources/design-icons/plan-icons/${name}`);
  return icon;
};
